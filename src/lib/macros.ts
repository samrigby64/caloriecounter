/** Shared macro colours — the iOS dark-mode system palette, matched across
 *  the rings, legend and meal columns. Vivid against true black. */
export const MACRO_COLORS = {
  kcal: '#0a84ff', // system blue
  protein: '#30d158', // system green
  carbs: '#ff9f0a', // system orange
  fat: '#ff375f', // system pink
} as const

/** Water tracker accent (iOS system cyan). */
export const WATER_COLOR = '#64d2ff'
